// The unique marker string the Node crasher throws, shared (side-effect free)
// between the crasher and the driver so the driver can find the exact issue.
export const MARKER = "crashlens node e2e uncaught marker";
